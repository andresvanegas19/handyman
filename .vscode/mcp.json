{
  "servers": {
    "convex-mcp": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "--no-install",
        "convex",
        "mcp",
        "start",
        "--project-dir",
        "${workspaceFolder}"
      ]
    }
  }
}
